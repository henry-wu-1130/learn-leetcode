// BUG
/**
 * @param {number} n
 * @return {string[]}
 */
var generateParenthesis = function (n) {
  // n level
  // n - 1 level
  //
  const result = [];
  // 3
  while (--n > 0) {}
};

generateParenthesis(4);
